<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- the base is parent -->
    <parent>
        <artifactId>sakai-upv2</artifactId>
        <groupId>org.sakaiproject</groupId>
		<version>${sakai.version}</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
   	<artifactId>sakai-upv2-hbm</artifactId>
	<groupId>org.sakaiproject</groupId>
	<name>sakai-upv2-hbm</name>
		<version>${sakai.version}</version>

	<!-- you must deploy your HBM files to shared or the global session factory cannot access them -->
	<packaging>jar</packaging>
	<properties>
		<deploy.target>shared</deploy.target>
	</properties>

	<dependencies>
		<!-- HBMs have no dependencies since they are not built -->
	</dependencies>

	<build>	
		<sourceDirectory>src/java</sourceDirectory>
		<resources>
			<resource>
			  <directory>src/java</directory>
			    <includes>
			      <include>**/*.xml</include>
			    </includes>
			</resource>
		</resources>     
	</build>

</project>

